@model IEnumerable<AspCoreEntityPostgres.Models.MemoSignatory>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Sign)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Memo.Thema)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Comments)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.User.UserFIO)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Sign)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Memo.Thema)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Comments)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User.UserFIO)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdMemoSignatory">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdMemoSignatory">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdMemoSignatory">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
